<resources>
    <!-- Stillwater — window bg matches ColorPage so there's no flash on launch -->
    <style name="Theme.Truffle" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/color_page</item>
        <item name="android:statusBarColor">@color/color_page</item>
        <item name="android:navigationBarColor">@color/color_page</item>
    </style>
</resources>
